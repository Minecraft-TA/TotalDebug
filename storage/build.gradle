plugins { id 'totaldebug.java-conventions' }
base { archivesName = 'totaldebug-storage' }
dependencies {
    // Each application supplies Gson in its own runtime.
    compileOnly libs.gson
    testImplementation libs.gson
}
